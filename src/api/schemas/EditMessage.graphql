mutation editMessage($id: Int!, $content: String!) {
  editMessage(id: $id, content: $content) {
    error {
      code
      message
    }
    message {
      chat_id
      content
      created_at
      id
    }
    success
  }
}