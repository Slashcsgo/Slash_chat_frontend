mutation modifyChat($id: Int!, $description: String, $title: String) {
  modifyChat(id: $id, description: $description, title: $title) {
    chat {
      description
      title
    }
    error {
      code
      message
    }
    success
  }
}